# 2026-08

- 2026-08-20 · [缺陷] 文件变更列表的大小列只显示当前文件大小，删除文件显示旧大小；所有文件的净字节变化集中显示在分组统计且低于 1 KB 时隐藏。暂存区与工作树差异分别从对应 Git 基线读取大小。范围：workspace
  原因：原实现只对工作树路径调用文件系统 `stat`，文件删除后路径不存在，无法显示更新前大小；同一文件同时存在暂存和未暂存差异时还会误用同一基线，分组标题也只有行数统计。
  当前依据：无。
  证据：`snapshot/git_status.go`、`snapshot/types.go`、`src/app/components/diff-list.tsx`、`snapshot/git_test.go`、`src/app/domain/repo-snapshot-merge.ts`。

- 2026-08-18 · [重构] 前端 Pi 协议观察、LF 粘贴资格和 ConPTY 鼠标兼容启停收拢到 `TerminalProtocolStateMachine`，终端表面只转交事件并执行状态机效果。范围：workspace
  原因：协议识别虽已集中，但鼠标启停、会话结束清理和粘贴资格仍由 600 余行的 `RepoTerminalSurface` 拼装，协议变化需要跨模块同步修改且无法通过同一公开测试面验证完整生命周期。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`src/app/components/terminal-protocol-observer.ts`、`src/app/components/repo-terminal-surface.tsx`、`src/app/components/terminal-protocol-observer.test.ts`。

- 2026-08-18 · [缺陷] Pi 终端会话结束后封住旧协议快照产生的鼠标兼容效果，直到下一会话重置状态机，关闭序列的 xterm 解析回调不再重新开启本地鼠标模式。范围：workspace
  原因：退出处理只清除了鼠标兼容激活位，关闭序列触发的异步 `onWriteParsed` 仍会命中退出前的完整 Pi 指纹，并立即产生相反的开启效果。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`src/app/components/terminal-protocol-observer.ts`、`src/app/components/terminal-protocol-observer.test.ts`。

- 2026-08-18 · [演进] 默认终端在切换离开所属仓库 1 分钟后，若从未成功提交非空命令或已被用户清空，则自动关闭表面和后端会话；Shell 提示符、路径与输出不计入内容。范围：workspace
  原因：默认终端会按仓库持续保留，长期使用会积累无法手动关闭的空终端进程；仅按 xterm 文本判断会把 Shell 初始提示符误判为已使用。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`src/app/components/repo-terminal-tab.tsx`、`src/app/components/repo-terminal-surface.tsx`、`src/app/features/terminal/terminal-command-tracker.ts`、`src/app/features/terminal/terminal-workspace.tsx` 及对应测试。

- 2026-08-17 · [功能] 内嵌终端识别 HTTP(S) URL、`file://` 本机 URL 和 Windows 绝对路径，用户按住 Ctrl 单击后分别交给系统浏览器或默认关联程序打开。范围：workspace
  原因：终端输出中的构建结果、日志和文件定位无法直接跳转，用户必须复制内容再手动打开。
  当前依据：`architecture/shared/terminal-runtime.md`、`architecture/shared/wails-bridge.md`。
  证据：`src/app/components/terminal-link-provider.ts`、`src/app/components/repo-terminal-surface.tsx`、`app.go`、`internal/desktop/client.go`。

- 2026-08-17 · [缺陷] Windows 内嵌终端的应用粘贴改为图片优先，并通过专用 Wails 绑定读取图片和文本；图片读取取消 `IsClipboardFormatAvailable` 前置判断，直接由 STA 数据对象读取器识别延迟渲染和注册 PNG 格式，空剪贴板、读取超时和原生失败均不再发送原始 `Ctrl+V` 伪装成功。范围：workspace
  原因：WebView 的异步剪贴板 API 可能永久不返回，原生 `paste` 事件可能不派发，Go 文本读取缺少 OS 线程亲和性会破坏 `OpenClipboard`/`CloseClipboard`，而图片格式预判又会对延迟渲染数据产生假阴性；这些跨层行为先后表现为输入队列永久阻塞、`0x16` 假成功、错误详情丢失和图片被误判为空。最终实证确认必须绕过 WebView 剪贴板，并让真正的 STA 图片读取器而非格式枚举决定是否存在图片。
  当前依据：`architecture/shared/terminal-runtime.md`、`architecture/shared/wails-bridge.md`、`requirements/adrs/003-pi-windows-terminal-protocol-delivery.md`。
  证据：`internal/desktop/clipboard_windows.go`、`app.go`、`src/app/infrastructure/wails-client.ts`、`src/app/infrastructure/wails-runtime.ts`、`src/app/components/repo-terminal-shortcuts.ts`、`src/app/components/repo-terminal-surface.tsx`、`src/app/components/terminal-input-trace.ts`及对应测试；真实 Windows 剪贴板图片成功导出为 `%TEMP%/pi-clipboard-*.png` 并写入 Pi。

- 2026-08-14 · [重构] 前端终端表面的会话启动、重启、事件绑定和释放收拢到 `TerminalSurfaceSession`；`RepoTerminalSurface` 只保留 xterm 显示、输入转发和协议观察。范围：workspace
  原因：会话生命周期顺序曾分散在运行时与 600 余行 UI 表面中，修改终端行为需要跨模块同步理解和验证。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`src/app/features/terminal/terminal-workspace.tsx`、`src/app/components/repo-terminal-surface.tsx`、`src/app/features/terminal/terminal-workspace.test.ts`。

- 2026-08-14 · [缺陷] Windows 图片粘贴识别标准位图和注册 `PNG`/`image/png` 格式，并对剪贴板短暂占用和图片延迟可用进行有界重试；检测到图片但未生成有效 PNG 时显式失败，已确认 Pi 会话读取为空时也不再回退为原始 `Ctrl+V` 并显示“已粘贴”。范围：workspace
  原因：应用图片预检只覆盖 `CF_BITMAP`/`CF_DIB`/`CF_DIBV5`，会把仅提供注册 PNG 数据流的图片判成空剪贴板；PowerShell 的 .NET 剪贴板读取异常还可能是非终止错误。Pi 文本粘贴切换为应用读取后，这个既有格式缺口开始直接影响已确认 Pi 会话，并触发无意义的 `Ctrl+V` 假成功。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`internal/desktop/clipboard_windows.go`、`internal/desktop/process_windows_test.go`、`internal/desktop/clipboard_windows_test.go`、`src/app/components/repo-terminal-shortcuts.ts`、`src/app/components/repo-terminal-shortcuts.test.ts`。

- 2026-08-13 · [缺陷] Windows 内嵌 Pi 的已确认身份跨 Pi 后台 cmd/npm 子进程临时改写的 OSC 0 标题保持，原始 LF 粘贴不再错误回退为会被 ConPTY 剥离边界的默认路径。范围：workspace
  原因：Pi 自身的更新检查会在同一 PTY 输出 cmd/npm 标题；观察器曾把这个非 Pi 标题当成 Pi 会话结束，尽管 bracketed paste 和增强键盘仍有效，导致多行粘贴又按 CR 逐行提交。
  当前依据：`architecture/shared/terminal-runtime.md`、`requirements/adrs/003-pi-windows-terminal-protocol-delivery.md`。
  证据：`src/app/components/terminal-protocol-observer.ts`、`src/app/components/terminal-protocol-observer.test.ts`、目标机 ConPTY 启动探针。

- 2026-08-13 · [重构] 定向 Git 状态快照与用户阅读会话形成明确边界。范围：workspace
  原因：各组件曾分别从完整 `RepoDetail` 对象推断失效，等价轮询容易触发重复初始化、加载和渲染；合并层现显式区分后台与交互更新，交互使用单调版本，后台使用同一次 Git status 返回的 `HEAD` 版本，既避免时钟竞态，也能识别终端产生的新提交。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/domain/repo-snapshot-merge.ts`、`src/app/components/workspace.tsx`、`src/app/components/repo-history-tab.tsx`、`src/app/features/diff/file-diff-loader.ts`。

- 2026-08-13 · [缺陷] 周期 Git 状态快照不再清空和重载历史页的提交列表、当前选择与提交详情。范围：workspace
  原因：后台状态快照有意省略历史数据，但历史页曾监听整个 `RepoDetail`，每次 2 秒对象替换都会把省略误判为历史失效并造成闪烁。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/components/repo-history-tab.tsx`。

- 2026-08-12 · [缺陷] 文件差异读取复用列表快照中的状态和未跟踪标记，普通点击从“再次扫描文件状态再读取差异”缩减为一次定向 Git 差异命令。范围：workspace
  原因：Windows 上单个 Git 进程实测可耗时数百毫秒，重复校验与 2 秒状态轮询重叠时会把每次点击放大到 3～5 秒。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`snapshot/file_diff.go`、`snapshot/git_status.go`、`src/app/infrastructure/wails-client.ts`。

- 2026-08-12 · [缺陷] 周期 Git 状态刷新不再按扫描时间重挂已展开的文件差异，也不会因等价 `FileChange` 对象替换而取消尚未完成的读取；加载器跨同仓库快照复用，并只在文件可观察变更摘要变化时失效缓存。范围：workspace
  原因：高频快照每次都会替换 `RepoDetail` 和 `FileChange` 对象且改变扫描时间，原实现因此丢弃缓存、反复清理读取 effect，并重置差异滚动位置，导致慢请求可能长期停在加载状态且打断阅读。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/components/workspace.tsx`、`src/app/components/diff-list.tsx`、`src/app/components/file-diff.tsx`、`src/app/features/diff/file-diff-loader.ts`。

- 2026-08-12 · [缺陷] 组合命令在切换项目后继续同步输出，并以命令会话身份阻止旧命令覆盖新命令或在手动清空后重新打开面板。范围：workspace
  原因：输出已是工作区级日志，却仍复用了项目作用域保护，导致组合继续执行时后续日志被静默丢弃。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/features/commands/repo-command-console.ts`、`src/app/features/commands/repo-command-console.test.ts`。

- 2026-08-13 · [缺陷] Windows 内嵌终端在已确认 Pi 会话中把文本粘贴的所有行结束符写为原始 LF，不再依赖 ConPTY 会剥离的 bracketed-paste 边界或修饰键 CSI，因此多行内容留在 Pi 编辑器而不逐行提交。范围：workspace
  原因：目标机直接 ConPTY→Node 探针证明 `ESC[200~...ESC[201~` 与伪造的 `Alt+V` 均未到达 stdin，而单次写入的 LF 保持为 `0a`；Pi 将 LF 作为新行输入。
  当前依据：`architecture/shared/terminal-runtime.md`、`requirements/adrs/003-pi-windows-terminal-protocol-delivery.md`。
  证据：`src/app/components/repo-terminal-shortcuts.ts`、`src/app/components/repo-terminal-surface.tsx`、`src/app/components/terminal-protocol-observer.ts`、`internal/terminal/conpty_input_windows_test.go`、对应测试。

- 2026-08-12 · [功能] 自动扫描增加分级本地状态刷新：当前选中项目立即刷新并每 2 秒检查，其余项目每 10 秒检查，远端仍按原设置间隔刷新。范围：workspace
  原因：终端开发时需要快速看到当前项目的文件和提交状态，同时避免对所有仓库高频执行远端请求。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/application/use-repo-status-polling.ts`、`src/app/application/snapshot-coordinator.ts`。

- 2026-08-12 · [功能] 主界面命令输出在切换项目后继续保留，只响应用户显式清空。范围：workspace
  原因：输出用于核对刚执行的命令结果，项目切换不应让日志主动消失。
  当前依据：`.codestable/architecture/INDEX.md`。
  证据：`src/app/features/commands/use-repo-command-panel.tsx`。

- 2026-08-11 · [功能] Windows 新建终端改为每次从系统和用户注册表重建环境变量，因此新增、更新或删除变量无需重启应用即可在新终端中生效。
  原因：已运行的桌面应用只继承启动时的进程环境，后续创建的 ConPTY 会话原先无法看到用户后来修改的环境变量。
  当前依据：`architecture/shared/terminal-runtime.md`。
  证据：`internal/terminal/host_windows.go`、`internal/terminal/host_windows_test.go`。

- 2026-08-04 · [缺陷] 为 Pi Windows 终端确定了有界的启动协议交付链路：后端在退出事件前冲刷原始输出，前端回放绑定前的输出和退出码；缓存溢出明确失败而非部分回放。Windows 快捷键仅在集中规则表中归一，协议诊断旁路观察 xterm 的受控粘贴和键盘协商状态。范围：workspace
  原因：启动输出丢失会使 Pi 无法开启 bracketed paste 或完成键盘协议协商，表现为 `Shift+Enter` 无效和多行粘贴逐行提交；无限缓存和真实终端时序测试又会隐藏或放大该问题。
  当前依据：`architecture/shared/terminal-runtime.md`、`requirements/adrs/003-pi-windows-terminal-protocol-delivery.md`。
  证据：`internal/terminal/manager.go`、`internal/terminal/manager_lifecycle_test.go`、`src/app/features/terminal/terminal-workspace.tsx`、`src/app/components/repo-terminal-shortcuts.ts`、`src/app/components/terminal-protocol-observer.ts`。

- 2026-08-04 · [缺陷] 设置弹窗中的即时目录和分类操作会同步更新当前草稿，后续保存不会覆盖刚添加的内容。范围：workspace
  原因：即时持久化与弹窗草稿彼此独立时，旧草稿会在保存时丢弃最新设置。
  当前依据：无。
  证据：`src/app/application/settings-actions.ts`、`src/app/components/settings-modal.tsx`、`src/app/application/settings-actions.test.ts`。

- 2026-08-04 · [功能] 支持按仓库路径停止或恢复监控，已停止监控的项目不会参与仓库发现和扫描。范围：workspace
  原因：扫描文件夹时用户需要排除其中不关心的个别项目，而不移除整个目录。
  当前依据：`requirements/CONTEXT.md`、`architecture/shared/wails-bridge.md`。
  证据：`snapshot/service.go`、`src/app/application/settings-actions.ts`、`src/app/components/repositories-settings-tab.tsx`。

- 2026-08-04 · [缺陷] 侧栏手动扫描改为请求完整仓库快照，因此会立即发现扫描根目录中新建的仓库。范围：workspace
  原因：原实现只逐个刷新已在侧栏中的仓库，新增仓库只能在重启时通过启动扫描发现。
  当前依据：`requirements/CONTEXT.md`。
  证据：`src/app/application/use-sidebar-scan.ts`、`src/app/application/use-sidebar-scan.test.ts`。

- 2026-08-03 · [缺陷] 组合操作切换项目后仍保留内部生成的提交说明，并继续对启动时的仓库执行后续步骤。范围：workspace
  原因：界面项目切换曾同时丢弃组合内部状态，导致“生成 → 提交”在提交阶段收到空说明而中断。
  当前依据：`requirements/CONTEXT.md`。
  证据：`src/app/features/commands/use-repo-command-panel.tsx`、`src/app/features/commands/combo-commit-message-state.ts`。

- 2026-08-07 · [缺陷] Windows 内嵌终端为 Pi 0.84.1 全屏补回被 ConPTY 丢弃的鼠标模式，使滚轮通过 xterm 原生 SGR 鼠标输入滚动 Pi 核心输出。范围：workspace
  原因：ConPTY 保留备用屏幕、受控粘贴和增强键盘控制字节，却丢弃 Pi 同次启动输出中的 DEC 鼠标模式，导致 xterm 把滚轮降级编码为上下方向键。
  当前依据：`architecture/shared/terminal-runtime.md`、`requirements/adrs/003-pi-windows-terminal-protocol-delivery.md`。
  证据：`src/app/components/repo-terminal-surface.tsx`、`src/app/components/terminal-protocol-observer.ts`、`src/app/components/terminal-protocol-observer.test.ts`。

- 2026-08-13 · [重构] 移除文件差异结果缓存，命令控制台输出改为有界缓冲，终端输入观测改为仅在观测弹窗打开时记录。范围：workspace
  原因：长时间使用下无界累积会导致内存持续增长与界面卡顿；文件差异读取已经足够快，不再需要结果缓存；输入观测是调试功能，平时无需记录。
  当前依据：`architecture/INDEX.md`（差异读取规则已更新）。
  证据：`src/app/features/diff/file-diff-loader.ts`、`src/app/features/commands/repo-command-console.ts`、`src/app/components/terminal-input-trace.ts`、`src/app/components/repo-terminal-surface.tsx`。
